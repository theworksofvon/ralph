# ============================================
# Default Anthropic Profile for Claude Code
# ============================================
#
# SETUP:
# 1. Copy this file: cp default.env.example default.env
# 2. Set your Anthropic API key below (or in your shell profile)
#
# USAGE:
#   ralph --profile default
#   source ~/.ralph/profiles/default.env && claude
# ============================================

# Unset any proxy overrides to use Anthropic directly
unset ANTHROPIC_BASE_URL
unset ANTHROPIC_AUTH_TOKEN

# Uncomment and set if not in your shell profile
# export ANTHROPIC_API_KEY="your-anthropic-key-here"
